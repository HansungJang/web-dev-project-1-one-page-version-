# contents cache 함수 구현 (by codex - 26.07.15. response my answer)

> #contents cache function 구현
> #current issue : [
> - elementsCache map이 call이 되지 않는 원인 / 해결방안 / 이슈의 원인과 솔루션 중학생도 이해할 수 있게 설명
>   ]
>
> User context
>
> Attachments:
> - main.js: `~/OneDrive/바탕 화면/New folder/onepage_version/main.js`

<details><summary>4 previous messages</summary>

> 문제의 원인을 코드 흐름에서 확인한 뒤, 왜 캐시가 호출되지 않는지와 고치는 방법을 쉽게 설명드리겠습니다.
>
> <details><summary>Ran 2 commands</summary>
>
> - Ran `Get-Content -Raw AGENTS.md; Get-Content -Raw COMMANDS.md; Get-Content -Raw FLAGS.md; Get-Content -Raw PRINCIPLES.md; Get-Content -Raw RULES.md; Get-Content -Raw MCP.md; Get-Content -Raw PERSONAS.md; Get-Content -Raw ORCHESTRATOR.md; Get-Content -Raw MODES.md`
> - Ran `Get-ChildItem -Path C:\Users\hansu\Documents\Codex -Filter AGENTS.md -Recurse -ErrorAction SilentlyContinue | Select-Object -ExpandProperty FullName; Get-Content -Raw 'C:\Users\hansu\OneDrive\바탕 화면\New folder\onepage_version\main.js'`
>
> </details>
>
> 코드상 `Map`은 선언돼 있지만, 현재는 홈 데이터에만 연결되어 있습니다. 관련 호출 경로와 저장 뒤 갱신까지 더 확인하겠습니다.
>
> <details><summary>Ran 2 commands</summary>
>
> - Ran `Get-Content -Raw 'C:\Users\hansu\.agents\skills\source-command-sc-analyze\SKILL.md'; Select-String -Path 'C:\Users\hansu\OneDrive\바탕 화면\New folder\onepage_version\main.js' -Pattern 'elementsCache|updateUI|get(Home|Center|Procedure|Apply|Experts|Location|Specialties)Content|save(Home|Center|Procedure|Apply|Experts|Location|Specialties)|loadData\(' -CaseSensitive | ForEach-Object { '{0}: {1}' -f $_.LineNumber, $_.Line.Trim() }`
> - Ran `$p='C:\Users\hansu\OneDrive\바탕 화면\New folder\onepage_version\main.js'; $lines=Get-Content $p; foreach($n in 180..270 + 930..960 + 1025..1038){ if($n -le $lines.Count){ '{0,4}: {1}' -f $n,$lines[$n-1] } }`
>
> </details>
</details>

원인은 명확합니다. `elementsCache`는 현재 **홈 페이지에서만** 사용됩니다.

- `getHomeContent()`에만 `elementsCache.has('home')`가 있습니다.
- `getCenterContent()`, `getApplyContent()` 등 나머지는 매번 Firestore를 호출합니다.
- `loadData()`의 `localStorage` 캐시와 `elementsCache`는 서로 다른 캐시인데, `loadData()` 호출은 주석 처리되어 있습니다.
- `Map` 캐시는 새로고침하면 사라집니다. 브라우저 메모리에만 있기 때문입니다.
- 저장 후 기존 캐시를 지우지 않아, 수정 전 내용이 다시 보일 수 있습니다.

중학생식 비유로 말하면, `elementsCache`는 “자주 쓰는 내용을 책상 서랍에 넣는 것”입니다. 그런데 지금은 홈 페이지 책만 서랍에 넣고, 다른 페이지 책들은 매번 도서관(Firestore)에 가서 빌리고 있습니다. 또 홈페이지를 새로고침하면 책상이 새로 만들어져 서랍도 비게 됩니다.

`main.js`의 현재 getter 함수들 부분을 아래처럼 바꾸면 모든 `contents` 페이지가 같은 캐시를 사용합니다.

```js
let elementsCache = new Map();

const CONTENT_DOCUMENTS = {
  home: 'index',
  apply: 'apply',
  center: 'center',
  procedure: 'procedure',
  experts: 'experts',
  location: 'location',
  specialties: 'specialties'
};

async function getContent(pageName) {
  // 1. 서랍(Map)에 있으면 Firestore에 가지 않음
  if (elementsCache.has(pageName)) {
    console.log(`${pageName}: cache에서 불러왔습니다.`);
    return elementsCache.get(pageName);
  }

  // 2. 서랍에 없을 때만 Firestore 호출
  const documentId = CONTENT_DOCUMENTS[pageName];
  const snap = await getDoc(doc(db, 'contents', documentId));

  if (!snap.exists()) {
    console.warn(`${pageName}: Firestore 문서가 없습니다.`);
    return null;
  }

  const data = snap.data();

  // 3. 받은 데이터를 서랍에 저장
  elementsCache.set(pageName, data);
  console.log(`${pageName}: Firestore에서 불러와 cache에 저장했습니다.`);

  return data;
}

window.getHomeContent = () => getContent('home');
window.getApplyContent = () => getContent('apply');
window.getCenterContent = () => getContent('center');
window.getProcedureContent = () => getContent('procedure');
window.getExpertsContent = () => getContent('experts');
window.getLocationContent = () => getContent('location');
window.getSpecialtiesContent = () => getContent('specialties');
```

그리고 각 저장 함수에서 `setDoc(...)` 직후 해당 캐시를 지워 주세요. 예를 들어 `saveHome` 안에서는:

```js
await setDoc(homeDocRef, { /* 저장할 데이터 */ });

// 이전 홈 데이터는 이제 오래된 정보이므로 삭제
elementsCache.delete('home');
```

`saveCenter`에는 `elementsCache.delete('center')`, `saveApplyContent`에는 `elementsCache.delete('apply')`처럼 적용하면 됩니다.

참고로 현재 파일에서는 `loadData()`가 주석 처리되어 있어 실행되지 않으며, 이것은 `elementsCache`와 별개인 `localStorage` 캐시입니다. 새로고침 뒤에도 캐시를 유지하려면 `Map`만으로는 부족하고, 지금의 `localStorage` 또는 Firebase의 `persistentLocalCache`를 함께 써야 합니다.

> yes / localstorage 활용이 맞는 것 같아
> -> 페이지 전환시 새로고침으로 map이 초기화 되기 때문에 수정이 필요한 부분 수정
> -> 현재 map의 cache 호출 문제 핵심 원인 확인 (피드백 및 수정안 제시할 것)

<details><summary>2 previous messages</summary>

> `Map` 대신 새로고침에도 남는 `localStorage` 캐시로 바꾸는 방향이 맞습니다. 캐시 흐름과 Firebase 동작을 함께 점검해, 실제로 막힌 지점과 교체 코드를 정리하겠습니다.
>
> <details><summary>Ran 2 commands, searched the web</summary>
>
> - Ran `Get-Content -Raw 'C:\Users\hansu\.agents\skills\source-command-sc-analyze\SKILL.md'; Select-String -Path 'C:\Users\hansu\OneDrive\바탕 화면\New folder\onepage_version\main.js' -Pattern 'function checkCurrentPage|window.checkCurrentPage|elementsCache|loadData\(|persistentLocalCache|setDoc\(' -CaseSensitive | ForEach-Object { '{0}: {1}' -f $_.LineNumber, $_.Line.Trim() }`
>
> <details><summary>Searched the web</summary>
>
> - Searched the web for `Firebase Firestore web persistentLocalCache getDoc cache server official documentation`
>
> </details>
>
> - Ran `$p='C:\Users\hansu\OneDrive\바탕 화면\New folder\onepage_version\main.js'; $lines=Get-Content -LiteralPath $p; $lines[830..855]`
>
> <details><summary>Searched the web</summary>
>
> - Searched the web for `site:firebase.google.com/docs/firestore/manage-data/enable-offline web persistentLocalCache ...`
>
> </details>
>
> </details>
</details>

핵심 원인은 `Map` 자체가 고장 난 것이 아니라, **현재 코드 구조에서 캐시가 필요한 경로에 연결되지 않았기 때문**입니다.

1. `elementsCache`는 `getHomeContent()`에만 적용되어 있습니다. 다른 페이지 getter는 항상 Firestore를 호출합니다.
2. 페이지 이동이 새로고침 방식이면 `main.js`가 다시 실행되고 `new Map()`도 새로 만들어져 캐시가 사라집니다.
3. 기존 `loadData()`는 `site/content` 문서를 읽지만, 실제 화면은 `contents/index`, `contents/center` 등을 읽습니다. 게다가 `loadData()` 호출도 주석 처리돼 있습니다. 즉, 이 캐시는 현재 화면 갱신 경로와 연결되어 있지 않습니다.
4. 저장 후 캐시를 지우지 않으면 이전 데이터가 남습니다.

참고로 이미 설정한 `persistentLocalCache()`는 Firestore 자체의 영구 캐시입니다. 다만 “24시간 동안은 Firestore 요청 자체를 건너뛴다”는 정책은 별도 `localStorage` 코드로 관리하는 편이 명확합니다. Firebase의 영구 캐시는 브라우저 재시작 뒤에도 남고, 서버와 동기화됩니다. [Firebase 공식 문서](https://firebase.google.com/docs/firestore/manage-data/enable-offline)

`let elementsCache = new Map();`부터 각 `get...Content()` 함수까지를 아래 코드로 교체하세요.

```js
const CONTENT_CACHE_PREFIX = 'onepage:contents:';
const CONTENT_CACHE_TTL = 24 * 60 * 60 * 1000; // 24시간

const CONTENT_DOCUMENTS = {
  home: 'index',
  apply: 'apply',
  center: 'center',
  procedure: 'procedure',
  experts: 'experts',
  location: 'location',
  specialties: 'specialties'
};

function getCacheKey(pageName) {
  return `${CONTENT_CACHE_PREFIX}${pageName}`;
}

function readContentCache(pageName) {
  try {
    const saved = localStorage.getItem(getCacheKey(pageName));
    if (!saved) return null;

    const cache = JSON.parse(saved);

    // 캐시 형식이 잘못됐거나 24시간이 지났으면 사용하지 않음
    if (!cache?.data || !cache?.savedAt) return null;
    if (Date.now() - cache.savedAt > CONTENT_CACHE_TTL) return null;

    return cache.data;
  } catch (error) {
    console.warn(`[cache] ${pageName} 캐시를 읽지 못했습니다.`, error);
    return null;
  }
}

function saveContentCache(pageName, data) {
  localStorage.setItem(
    getCacheKey(pageName),
    JSON.stringify({
      savedAt: Date.now(),
      data
    })
  );
}

function clearContentCache(pageName) {
  localStorage.removeItem(getCacheKey(pageName));
}

async function getContent(pageName) {
  // 1. 새로고침 후에도 localStorage 캐시를 먼저 확인
  const cachedData = readContentCache(pageName);

  if (cachedData) {
    console.log(`[${pageName}] localStorage cache 사용`);
    return cachedData;
  }

  // 2. 캐시가 없거나 만료됐을 때만 Firestore 호출
  try {
    const documentId = CONTENT_DOCUMENTS[pageName];
    const snap = await getDoc(doc(db, 'contents', documentId));

    if (!snap.exists()) {
      console.warn(`[${pageName}] Firestore 문서가 없습니다.`);
      return null;
    }

    const data = snap.data();

    // 3. 받아온 데이터를 다음 페이지 이동을 위해 저장
    saveContentCache(pageName, data);

    console.log(`[${pageName}] Firestore에서 불러와 localStorage에 저장`);
    return data;
  } catch (error) {
    console.error(`[${pageName}] 콘텐츠 로드 실패:`, error);
    return null;
  }
}

window.getHomeContent = () => getContent('home');
window.getApplyContent = () => getContent('apply');
window.getCenterContent = () => getContent('center');
window.getProcedureContent = () => getContent('procedure');
window.getExpertsContent = () => getContent('experts');
window.getLocationContent = () => getContent('location');
window.getSpecialtiesContent = () => getContent('specialties');
```

그리고 각 저장 함수의 `await setDoc(...)` 바로 다음에 해당 페이지 캐시를 삭제하세요.

```js
// saveHome 내부
clearContentCache('home');

// saveCenter 내부
clearContentCache('center');

// saveProcedure 내부
clearContentCache('procedure');

// saveApplyContent 내부
clearContentCache('apply');

// saveExpertsContent 내부
clearContentCache('experts');

// saveLocationContent 내부
clearContentCache('location');

// saveSpecialtiesContent 내부
clearContentCache('specialties');
```

이렇게 되면 동작은 다음과 같습니다.

```text
첫 방문       → Firestore 요청 → localStorage 저장
페이지 이동   → localStorage 읽기 → Firestore 요청 생략
새로고침      → localStorage 읽기 → 캐시 유지
24시간 경과   → Firestore 재요청 → 캐시 갱신
관리자 저장   → 해당 캐시 삭제 → 다음 화면에서 최신값 요청
```

추가로 기존 `loadData()`는 현재 `updateUI()`와 다른 문서 경로를 읽으므로 제거하거나, 위 `getContent()` 방식으로 통합하는 것이 좋습니다.
